server:
  port: 8084
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
            uri: https://github.com/romelgutierrez/config-data.git
            search-paths: config-data
            username: romelguierrez
            password: rgo.54321
            default-label: main
  security:
    user:
      name: root
      password: s3cr3t
